# Workspace Rules

- **No Auto-Git**: Do NOT execute any git version control commands automatically (such as `git add`, `git commit`, `git push`, etc.). The user will perform all Git staging, commits, and pushes manually.
